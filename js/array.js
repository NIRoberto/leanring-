let language = [1, "html", 2, "css", 3, "js", "ruby"];

console.log(language);

//array method
let res = language.join(",");
let resu = language.indexOf(1);
let dele = language.pop(",");

console.log(res, resu, dele);
console.log(language);
